import 'package:flutter/material.dart';

class AnnouncementLogView extends StatelessWidget {
  const AnnouncementLogView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Duyurular")),
    );
  }
}
