import 'package:flutter/material.dart';

class HomeworkHistory extends StatelessWidget {
  const HomeworkHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Geçmiş Ödevler")),
    );
  }
}
